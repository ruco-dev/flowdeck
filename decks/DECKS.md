# Decks

The deck registry lives at the repo root: see [../DECKS.md](../DECKS.md).

This file used to be a duplicate of it and drifted; it is now a pointer so there
is a single registry to maintain. `tools/decks-lint.mjs` checks the root file
(and the gitignored `LOCAL-DECKS.md` for local decks, when present).
